# Configuración de producción para Render con Supabase
server.port=${PORT:8080}
server.servlet.context-path=/api/v1

# Configuración de base de datos Supabase (valores directos para producción)
spring.datasource.url=jdbc:postgresql://db.dtalvtuvoquygqzkpgqa.supabase.co:5432/postgres?sslmode=require&sslcert=&sslkey=&sslrootcert=
spring.datasource.username=postgres
spring.datasource.password=3HsODjsEVdtDwiaj
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate para producción
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuración adicional para conexión de base de datos
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=1

# Configuración adicional para SSL y conexión segura
spring.datasource.hikari.connection-test-query=SELECT 1
spring.datasource.hikari.validation-timeout=3000
spring.datasource.hikari.leak-detection-threshold=60000

# Deshabilitar inicialización de datos en producción
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=false
spring.sql.init.continue-on-error=false

# Configuración de correo electrónico (usar variables de entorno)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.transport.protocol=smtp

# Configuración de logging para producción
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.com.vocacional.prestamoinso=INFO

# Configuración de CORS para producción
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://agileloans.netlify.app}

# Configuración de seguridad
spring.security.require-ssl=false